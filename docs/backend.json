
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user of the CraftyLink application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        }
      },
      "required": ["id", "email"]
    },
    "SavedComparisonPage": {
      "title": "SavedComparisonPage",
      "type": "object",
      "description": "Represents a saved comparison page configuration for a user.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Reference to User. (Relationship: User 1:N SavedComparisonPage)"
        },
        "name": {
          "type": "string",
          "description": "Name of the saved comparison page."
        },
        "color": {
          "type": "string",
          "description": "Color tag for the saved item."
        },
        "keywords": {
          "type": "array",
          "description": "List of keywords for the comparison.",
          "items": { "type": "string" }
        },
        "date": {
            "type": "string",
            "format": "date-time",
            "description": "The date the comparison was saved."
        }
      },
      "required": ["userId", "name", "color", "keywords", "date"]
    }
  },
  "auth": {
    "providers": ["password", "anonymous", "google.com"]
  },
  "firestore": {
    "/users/{userId}/savedComparisonPages/{savedComparisonPageId}": {
      "schema": {
        "$ref": "#/entities/SavedComparisonPage"
      },
      "description": "Stores saved comparison page configurations for a specific user. Access is restricted to the owning user."
    }
  }
}
